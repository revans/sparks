ENV["RACK_ENV"] ||= "development"
require 'pathname'
require 'logger'
require 'bundler'
Bundler.require(:default, ENV["RACK_ENV"])
Bundler.require(:default, Sparks.env)

require "./config/application"
require "./app/api"

if Sparks.development?
  require 'rack-livereload'
  use Rack::LiveReload
end

run <%= module_classname %>::Api.run!
